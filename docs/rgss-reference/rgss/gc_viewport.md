# Viewport


ビューポートのクラス。画面の一部にスプライトを表示し、他の部分に はみ出さないようにしたい場合に使用します。

## スーパークラス


- [Object](sc_object.md)


## クラスメソッド



### Viewport.new(*x*, *y*, *width*, *height*)


### Viewport.new(*rect*)


### Viewport.new (RGSS3)



Viewport オブジェクトを生成します。

引数を指定しない場合は画面と同じサイズになります。 (RGSS3)

## メソッド



### dispose



ビューポートを解放します。すでに解放されている場合は何も行いません。

この操作により、関連付けられている他のオブジェクトが自動的に解放される ことはありません。

### disposed?



ビューポートがすでに解放されている場合に真を返します。

### flash(*color*, *duration*)



ビューポートのフラッシュを開始します。*duration* は フラッシュにかけるフレーム数です。

*color* に nil を指定した場合は、フラッシュの時間分 ビューポート自体を消去します。

### update



ビューポートのフラッシュを進めます。 このメソッドは、原則として 1 フレームに 1 回呼び出します。

フラッシュの必要がない場合は呼び出さなくても構いません。

## プロパティ



### rect



ビューポートとして設定する矩形 ([Rect](gc_rect.md)) です。

### visible



ビューポートの可視状態です。真のとき可視になります。初期値は true です。

### z



ビューポートの Z 座標です。この値が大きいものほど手前に表示されます。

Z 座標が同一の場合は後に生成されたオブジェクトほど手前に表示されます。

### ox



ビューポートの転送元原点の X 座標です。 この値を変化させることによって画面のシェイクなどを行います。

### oy



ビューポートの転送元原点の Y 座標です。 この値を変化させることによって画面のシェイクなどを行います。

### color



ビューポートにブレンドする色 ([Color](gc_color.md)) です。 ブレンドの割合にはアルファ値が使用されます。

[flash](#flash) によってブレンドされる色とは別に 管理されます。

### tone



ビューポートの色調 ([Tone](gc_tone.md)) です。

######
